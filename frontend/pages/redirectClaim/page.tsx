import { Assistant } from "@/components/main/assistant";
import RedirectToClaim from "@/components/main/redirectClaim";

export const RedirectToClaimPage = () => {
  return (
    <div>
      <RedirectToClaim />
      <Assistant />
    </div>
  );
};
